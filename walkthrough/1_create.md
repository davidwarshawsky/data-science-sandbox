# Step 1: Create Your First Sandbox

Welcome to the **Immutable Regulatory Sandbox**. Let's start by creating a protected environment.

This will generate a folder with a hardened **Dev Container** configuration.

[Create Sandbox](command:immutable.createSandbox)
